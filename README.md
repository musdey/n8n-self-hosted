# n8n-self-hosted
Docker Compose setup to self-host n8n on your own server with Nginx reverse proxy and automatic SSL certificates via Let’s Encrypt (Certbot). Includes persistent storage, basic authentication, HTTPS, and automatic certificate renewal.
